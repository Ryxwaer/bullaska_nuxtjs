version: '3.8'

services:
  bullaska:
    container_name: bullaska
    build:
      context: .
    image: bullaska:1.0
    networks:
      - nginx-proxy-manager_default

networks:
  nginx-proxy-manager_default:
    external: true
